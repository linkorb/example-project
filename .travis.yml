# Project language
language: php

sudo: required

services:
  - docker

cache:
  directories:
    - $HOME/.composer/cache/files

matrix:
  fast_finish: true
  include:
    - php: 5.6
    - php: 7.0

script:
  - make all

after_success:
  - make docker-publish
